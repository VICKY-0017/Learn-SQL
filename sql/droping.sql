DROP TABLE table_name; -- for droping the table

TURNCATE TABLE table_name; -- for turncating the data inside the table , but not the whole table

ALTER TABLE table_name -- to add an email column to the table_name
ADD Email varchar(225);


ALTER TABLE table_name
DROP COLUMN column_name --to delete a column in the table

ALTER TABLE table_name add(job varchar2(20)); -- to add a new column in the table
